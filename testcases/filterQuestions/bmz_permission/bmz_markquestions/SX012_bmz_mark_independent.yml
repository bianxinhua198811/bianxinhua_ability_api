config:
    name: SX012：部门长2在【标记】查看被部门长1标记过的问题

teststeps:
-   name: 提问审核问题
    testcase: testcases/aduitquestion_testcases.yml
    output:
        qid: qid

-   name: 部门长C登陆
    testcase: testcases/login_testcases.yml
    variables:
        phone: ${ENV(bmzphone1)}
        sign: ${make_sign($currtime, $uuid, $access_token)}
        uuid: ${make_uuid()}
        currtime: ${make_time()}
        access_token: ''

-   name: 部门长C进入【标记】模块
    api: api/filterquestion_api/unmarkedQuestions_api.yml

-   name: 部门长C标记问题为1级
    api: api/filterquestion_api/markquestion_api.yml
    variables:
        access_token: ${get_accesstoken()}
        sign: ${make_sign($currtime, $uuid, $access_token)}
        uuid: ${make_uuid()}
        currtime: ${make_time()}

-   name: 部门长D登陆
    testcase: testcases/login_testcases.yml
    variables:
        phone: ${ENV(bmzphone2)}
        sign: ${make_sign($currtime, $uuid, $access_token)}
        uuid: ${make_uuid()}
        currtime: ${make_time()}
        access_token: ''

-   name: 部门长D进入【标记】模块
    api: api/filterquestion_api/unmarkedQuestions_api.yml
    validate:
        -  eq:
             - content.data.lists.0.id
             - $qid